# SCENE DESIGN / СКЕЛЕТ (наследие нитей) V1
# Волна 0. Группируешь кадры в сцены + нити. Без внешности/планов/фаз action.

---

## РОЛЬ

Разбивка уже дала кадры. Ты группируешь их в сцены, ведёшь нити
(место / предметы / персонажи / открытые обещания) и отдаёшь карточку каждой сцены.

Вход после этого файла: ПОЛНЫЙ ЗАКАДР, КАДРЫ `{uuid,number,закадр,длительность}`, ОБЩИЙ ПЛАН.

Один ответ = один JSON. Без markdown, без комментариев `/* */`.

---

## КОНТРАКТ СЦЕНЫ (минимум полей)

`id_scene` (`scene_01`…), `кадры` (номера 1..N, каждый ровно в одной сцене),
`нити` (`нить`,`решение`∈продолжаю|закрыто|не моё,`как`),
`связь_с_прошлой` (`тип`), `суть`, `главное` (тип/глагол/субъект/объект/изменение/якорь_в_кадрах),
`биты` (можно `[]`), `персонажи` (`id`,`как`), `место_id`, `время`, `длительность_сек`
≈ сумма длительностей кадров сцены.

---

## ПРАВИЛА

- Локации: dedup → `locNN`; 1 кадр без возврата → `фон_разовый`.
- Персонажи: koref → один `cNN`; внешность НЕ писать.
- Связь: продолжение / сдвиг_времени / новое_место.
- Критик: ≤5 issues; в ответе финал после правок.

---

## ВЫХОД

Ключ **`scenes` обязателен и НЕПУСТОЙ**. Пустой `[]` = отказ.

```json
{
  "map": {"кадры": [{"number": 1, "места": ["мастерская"], "время": [], "персонажи": ["Павел"], "предметы": ["часы"]}]},
  "scenes": [{
    "id_scene": "scene_01",
    "кадры": [1],
    "нити": [],
    "связь_с_прошлой": {"тип": "новое_место"},
    "суть": "Павел берётся чинить часы",
    "главное": {"тип": "действие", "глагол": "починить", "субъект": "c01", "объект": "часы", "изменение": "нет→есть обещание", "якорь_в_кадрах": "починить", "нить": "починка"},
    "биты": [],
    "персонажи": [{"id": "c01", "как": "действует"}],
    "место_id": "loc02",
    "время": "день",
    "длительность_сек": 8.0
  }],
  "наследие_финал": {"место": "loc02", "предметы": ["часы"], "персонажи": ["c01"], "открыто": ["починка"]},
  "characters_seed": [{"id": "c01", "якорь": "Павел", "роль": "механик", "вне_кадра": false}],
  "locations_seed": [{"id": "loc02", "якорь": "мастерская", "фон_разовый": false}],
  "critic": {"issues": [], "accepted": true}
}
```

Пиши компактно: короткие строки, без прозы вне JSON.
