# Доп. работа с Excel #3

## 1. ТЕХНИЧЕСКАЯ ЧАСТЬ
- Откуда читаю: приложенный `project.xlsx` в его текущем состоянии, лист «{{VAR:ENRICH_3_SHEET}}».
- Куда пишу: **обязательно** обновлённый `project.xlsx` целиком, приложенный файлом в ответ — без вложения шаг уйдёт в retry-loop и не завершится.
- На что обратить внимание: не переименовывать листы, не удалять и не сдвигать уже заполненные строки/колонки — редактировать только то, что описано в задаче ниже.

## 2. РОЛЬ И ЗАДАЧА
{{VAR:ENRICH_3_TASK}}

## 3. ПРАВИЛА РЕДАКТИРОВАНИЯ
- Работай только с листом «{{VAR:ENRICH_3_SHEET}}».
- Сохраняй уже заполненные ячейки, если задача явно не требует их менять.
- Если каких-то данных не хватает — оставь ячейку пустой, не выдумывай значения.

## 4. ОГРАНИЧЕНИЯ
Не добавляй данных, не относящихся к задаче из блока 2. Не меняй названия листов и шапку таблицы. Не удаляй существующие формулы/стили, если это не часть задачи.

## 5. ФОРМАТ ВЫВОДА
Приложи обновлённый project.xlsx как .xlsx-файл (полный файл). Кратко текстом опиши, что именно изменил.
