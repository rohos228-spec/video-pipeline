# Промт-проверка: АУДИО (шаг 10)

> ⚠️ ЭТО ЗАГЛУШКА. Замени её на реальный промт перед запуском Фазы 7 пайплайна.

Контракт ответа (читается парсером `app/services/gpt_check.py`):

* Если аудио одобрено — ответь свободным текстом, содержащим слово
  **«одобрено»** (или `approved`).
* Если что-то не так и нужно перегенерировать — ответь фразой,
  начинающейся со слова **«перегенерация»** (или `regenerate`), и
  инструкцией. Пайплайн крутит озвучку заново (до 5 ретраев).
