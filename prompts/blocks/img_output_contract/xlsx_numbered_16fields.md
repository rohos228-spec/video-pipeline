Формат вывода строго:

```
[номер]. [готовый промт]
```

Без пояснений, списков и комментариев.
Один промт на одну ячейку закадрового текста.

Структура внутри промта (в этом порядке): тип сцены → сюжет → план → ракурс → фокус → персонажи → место → свет → передний/средний/задний план → палитра → стиль → `--no`.
Соотношение сторон: {{VAR:ASPECT_RATIO_VIDEO}}.
Длина: {{VAR:PROMPT_LEN_MIN}}–{{VAR:PROMPT_LEN_MAX}} символов.
