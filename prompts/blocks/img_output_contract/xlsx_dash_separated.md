В ответе только готовые промты без пояснений и рассуждений.
Каждый промт = один фрагмент входа, порядок сохраняется.
Промты разделяются знаком «-» на отдельной строке.
Без заголовков, нумерации и служебного текста.

Каждый промт включает блок `--no` в конце.
Длина: {{VAR:PROMPT_LEN_MIN}}–{{VAR:PROMPT_LEN_MAX}} символов. Формат: {{VAR:ASPECT_RATIO_VIDEO}}.
