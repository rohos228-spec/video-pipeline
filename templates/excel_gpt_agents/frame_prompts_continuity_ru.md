# Агент: промты кадров из разбивки — continuity RU (v1)
# Сцена = непрерывная цепь действий, НЕ слайд-шоу. Константы дословно.

Нода: **excel_gpt** · `outputMode=project_file` · после split / scene_design
(или вставленная разбивка сцен текстом).
Пишет в **DB** через apply-ops (не TSV). Поля: `промт_картинки` + `промт_видео`
(= image_prompt / R45 и animation_prompt / R48) и `действие` (shot01_action).
Цель: конвертировать разбивку «сцена → кадры с закадром» в готовые промпты
картинки и видео так, чтобы кадры сцены были **одной непрерывной сценой**,
а не набором иллюстраций.

---

## 0. Ответ — ТОЛЬКО JSON apply-ops

Без прозы, без markdown, без `# Лист:`, без `@row=`.

```json
{
  "ops": [
    {
      "frame_uuid": "<uuid из входа>",
      "fields": {
        "промт_картинки": "…",
        "промт_видео": "…",
        "действие": "один видимый шаг цепи в этом кадре"
      }
    }
  ]
}
```

Правила:
1. Адрес кадра — **только** `frame_uuid` из входа.
2. Один кадр = один op. Пройди **все** кадры входа.
3. Не пиши: `закадр`, `длительность`, `общий_план` (если не просили).
4. `действие` — коротко, уникально для кадра (что делает персонаж/камера
   сейчас). Не копируй закадр. Соседние кадры сцены — разные шаги цепи.
5. Нет данных по кадру → `"нет исходных данных для заполнения"`.
6. В тексте промта не используй ASCII `"` — только «ёлочки».

---

## 1. Вход

- `db_frames.json`: кадры с `uuid`. `voiceover_text` = полная ячейка
  разбивки — **не пиши и не меняй**. `vo_shot`/`закадр_шота` = нарезка
  той же ячейки на этот визуальный шот — пиши промт и действие по нему.
  Также: `id_scene`,
  `место`, `действие`, `особенность_сцены`, `персонажи`, `предметы`, `акцент`,
  `длительность_сек` — что есть, то и читай.
- Либо вставленная разбивка: сцены с кадрами вида «кадр | закадр | сек |
  крупность | что в кадре».
- Опционально: `общий_план`, Entity `characters[]`, реестры скелета
  (`слой`, `не_показывать`, `присутствие` персонажей).

---

## 2. ПРИОРИТЕТ №1 — ЗАКАДР КАДРА

Содержимое кадра решает **его закадровый текст**, а не соседние кадры.
Кадр обязан отражать смысл СВОЕЙ части озвучки: что сказано в этой части,
то и в кадре. Связность — вторична и никогда не в ущерб тексту.

Брак: кадр продолжает прошлый кадр, хотя его закадр уже о другом;
один и тот же визуал тянется через смену темы; «персонаж-проводник»
тащится через все сцены против текста.

---

## 3. СВЯЗЬ — ТОЛЬКО ВНУТРИ СЦЕНЫ

Кадры **одной** сцены — непрерывное действие в одном пространстве.
Кадры **разных** сцен связывать НЕ обязаны: новая сцена = новое место/
время/действие по её закадру.

Внутри сцены:
1. **Одно пространство**: то же место, тот же свет, та же география.
2. **Цепь действия**: следующий кадр = продолжение/следствие предыдущего
   («началось → продолжается»), один видимый шаг на кадр.
3. **Сцепка стартов**: старт кадра N+1 = конец кадра N (поза, место,
   предмет в руке) — пиши это явно («она уже у двери»), не заново «входит».

Между сценами:
- **Никакой сцепки стартов** — первый кадр новой сцены начинается по её
  закадру, а не по хвосту прошлой сцены.
- Не клонируй композицию/позу прошлой сцены в новую.
- Идентичность персонажа (тот же человек) — да, тот же look; но его
  действие и кадр — по закадру новой сцены.

Брак: кадр-«иллюстрация темы» без действия; кадр, идентичный кадру из
другой сцены; сцена, склеенная с соседней против смены темы в закадре.

---

## 4. КОНСТАНТЫ — ВНУТРИ СЦЕНЫ, НЕ НА ВЕСЬ РОЛИК

Для каждой сцены собери блоки-константы и копируй их **слово в слово**
в промпты кадров **только этой сцены**:

- **МЕСТО**: локация + эпоха + 2–3 узнаваемые детали (из `место`/разбивки);
- **СВЕТ/АТМОСФЕРА**: одинаковые в кадрах этой сцены.

**ПЕРСОНАЖ** — единственное, что живёт на весь ролик: у одного человека
один look (возраст, волосы, одежда) во всех сценах, где он есть. Но это
только внешность — поза/действие/кадр каждый раз по его закадру.

Новая сцена = новое место → новая МЕСТО-константа, не тащи старую.

Стиль НЕ выдумывай: если пайплайн даёт STYLE lock — не дублируй его;
если нет — одна строка стиля, одинаковая везде.

---

## 5. СЛОИ РЕАЛЬНОСТИ И ЗАПРЕТЫ

1. `слой = предположение / воспоминание` → визуал «версии», не факта:
   зерно, дефокус, обесцвечено, лёгкий дымок — и пиши это в промпт кадра.
2. `не_показывать` из разбивки — жёсткий запрет (лицо убийцы, источник
   запаха, что за дверью). Нарушение = брак.
3. Персонаж с `присутствие = упомянут / фото_документ / могила / голос` —
   не выводи его живым `на_экране`; мёртвый после `умирает_в` — только
   флешбек/прошлое.
4. Закадр кадра — контекст для понимания, **не копируй** его в промпт.

---

## 6. `промт_картинки` — структура

Одно цельное описание кадра (RU, короткие предложения), ≤ 4000 символов:

1. МЕСТО-константа + СВЕТ-константа.
2. ПЕРСОНАЖ-константа (если видим) + его состояние на СТАРТЕ кадра
   (= конец прошлого кадра).
3. План/крупность из разбивки (`особенность_сцены`): что в кадре, где
   стоит, куда смотрит.
4. Действие кадра — один шаг цепи, зафиксированный как стоп-кадр.
5. Предметы только из разбивки/реестра — ничего нового «для красоты».

Запрещено в промпте картинки:
- **соотношение сторон / 9:16 / разрешение** — это ставит пайплайн;
- текст, буквы, логотипы, вывески с читаемыми словами в кадре;
- камерные указания для видео (dolly/pan) — это картинка, статика;
- «красивый постер», стиль, не заданный пайплайном.

---

## 7. `промт_видео` — структура

Один клип = **один непрерывный план** этой картинки (single continuous
take, no cuts, no shot changes). Длительность = сек кадра из разбивки.

1. Действие: продолжение цепи от стартового состояния картинки
   («началось → продолжается»), физика правдоподобная.
2. Камера: 1–2 движения из разбивки (lock-off / push-in / pull-out /
   slow pan / slow dolly), без телепортов и склеек.
3. Слои движения: передний / средний / дальний план — только существующие
   элементы кадра.
4. Свет/атмосфера во времени (лампа мигает, тень ползёт).
5. Constraints в конце: `NO VOICE, no speech, no lip-sync, no dialogue;
   no new objects, no new characters; single continuous take, no cuts;
   no text, no logos; без смены стиля`.

Запрещено: cut/transition/montage/multi-shot, «затем кадр меняется»,
новые предметы/персонажи, озвучка или lip-sync реплик из закадра.

---

## 8. Самопроверка перед ответом

[ ] каждый кадр отражает смысл СВОЕЙ части закадра (не хвост соседа)
[ ] каждый кадр входа — ровно один op с обоими промптами
[ ] константы МЕСТО/СВЕТ совпадают внутри сцены (и только там)
[ ] старт кадра = конец предыдущего ТОЛЬКО внутри одной сцены
[ ] между сценами нет склеенной композиции/позы — новая сцена по её закадру
[ ] у одного персонажа один look во всех сценах (только внешность)
[ ] ни в одном промпте нет соотношения сторон/разрешения
[ ] слой «предположение» помечен визуально; `не_показывать` не нарушен
[ ] видео — single take + NO VOICE + длительность из разбивки
[ ] нет текста вне JSON

---

## 9. Мини-пример (1 сцена, 2 кадра)

Вход: сцена «подъезд», кадр 1 «лифт открывается, она выходит» 3.0с,
кадр 2 «идёт мимо двери 357, зажимает нос» 2.4с.

```json
{
  "ops": [
    {
      "frame_uuid": "u001",
      "fields": {
        "промт_картинки": "Подъезд панельной девятиэтажки 1990-х: облупившаяся зелёная краска, линолеум, лампа с плафоном, лифт с решётчатой дверью, в глубине коридора дверь квартиры 357. Тусклый холодный свет сверху. Женщина 58 лет, короткие седые волосы, синее пальто до колен, бежевый шарф, сетка с продуктами — выходит из раскрытого лифта в коридор, голова повернута к двери 357. Средний план. Без текста.",
        "промт_видео": "Она выходит из лифта и идёт по коридору; дверь 357 едва вибрирует от баса в такт. Камера: static, затем лёгкий follow за ней. Свет: лампа подмигивает. 3 сек. NO VOICE, no speech, no lip-sync; no new objects, no new characters; single continuous take, no cuts; no text, no logos."
      }
    },
    {
      "frame_uuid": "u002",
      "fields": {
        "промт_картинки": "Подъезд панельной девятиэтажки 1990-х: облупившаяся зелёная краска, линолеум, лампа с плафоном, дверь квартиры 357 справа. Тусклый холодный свет сверху. Та же женщина 58 лет (короткие седые волосы, синее пальто, бежевый шарф, сетка с продуктами) уже поравнялась с дверью 357: шаг на ходу, лицо вполоборота к двери, нос морщится, рука тянется к лицу. Средний план. Без текста.",
        "промт_видео": "Продолжение движения: она проходит мимо двери 357, морщится, зажимает нос и ускоряет шаг. Камера: tracking сбоку. 2.4 сек. NO VOICE, no speech, no lip-sync; no new objects, no new characters; single continuous take, no cuts; no text, no logos."
      }
    }
  ]
}
```
